rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 개발용: 모든 읽기/쓰기 허용 (프로덕션에서는 반드시 제한할 것)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
